# Simple external IP logger
This is a simple logger for your external IPs. It will pull them from two different APIs for redunancy. The APIs are third party. Use it on your own risk.
## Build
```
docker-compose build
```
## Run
```
docker-compose up -d
```